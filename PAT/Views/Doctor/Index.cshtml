@{
    ViewBag.Title = "Index";
}

<h2>Welcome Doctor</h2>

@using (Html.BeginForm())
{

    <div class="container">
        <div class="row text-center">
            <div class="col-md-6 table-bordered" style="min-height: 100px;">
                <div class="container-fluid">
                    <div class="card-block">
                        <h4 class="card-title" style="margin-top:40px">@Html.ActionLink("Click here to view the patients", "ViewPatients")</h4>
                    </div>
                </div>
            </div>

            <div class="col-md-6 table-bordered" style="min-height: 100px;">
                <div class="container-fluid">
                    <div class="card-block">
                        <h4 class="card-title" style="margin-top:40px">@Html.ActionLink("Diet Recommendation", "Create", "DietRecommendations")</h4>
                    </div>
                </div>
            </div>


        </div>
    </div>
}