@{ try
    {
        if (TempData["Error"] != null)
        {
            <script>
                Swal.fire('PTS', '@TempData["Error"]', '@TempData["msgType"]');
            </script>

        }
    }
    catch { }
}